# Initial Context

The user wants to build a personal AI agent project from the ground up. The project is in a seed stage, so many requirements will emerge through conversation.

Important collaboration expectation: key information should be written into local files when it affects future work. Do not rely only on chat history for project rules, architectural decisions, or user preferences.

Multilingual content should be maintained in separate language files instead of mixing Chinese and English in one long-term document.

