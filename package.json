{
  "name": "xiaomi-agent-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Electron GUI workbench for multi-role agent orchestration.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @xiaomi/desktop dev",
    "build": "pnpm --filter @xiaomi/desktop build",
    "postinstall": "pnpm --filter @xiaomi/desktop rebuild:native",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
